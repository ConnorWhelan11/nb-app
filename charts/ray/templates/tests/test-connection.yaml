apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ray.fullname" . }}-test-connection"
  labels:
    {{- include "ray.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ray.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
